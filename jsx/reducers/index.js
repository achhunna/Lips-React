import {action} from '../actions';

export const reducer = (dispatch, action) => {
    /*
    switch(action.type) {
        case "test_action":
            return {...dispatch, action.payLoad};
    }
    */
    return null;
};
